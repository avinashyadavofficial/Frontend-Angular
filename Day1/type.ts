type Person = {
  name: string;
  age: number;
  isStudent: boolean;
};

const person: Person = {
  name: "Avinash",
  age: 22,
  isStudent: false,
};

console.log(person.name); // Avinash
console.log(person.age);  // 22